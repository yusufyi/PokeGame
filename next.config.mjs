/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "img.pokemondb.net",
      "https://raw.githubusercontent.com/PokeAPI/",
    ],
  },
};

export default nextConfig;
